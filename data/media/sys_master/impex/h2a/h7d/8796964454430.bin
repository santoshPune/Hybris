### Start: LOESCHE-19 ###

# Rename BaseStore #
INSERT_UPDATE BaseStore;uid[unique=true];defaultCurrency(isocode);name[lang=de];name[lang=en];taxGroup(code);currencies(isocode);pointsOfService(name);
;aitsBaseStore;EUR;aits;aits;de-taxes;EUR;